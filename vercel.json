{
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/node",
      "config": {
        "includeFiles": ["./**"],
        "installCommand": "npm install --legacy-peer-deps"
      }
    }
  ]
}
